@{ Layout.Title = T("Create New Hierarchy").ToString(); }

@foreach (var type in Model.HierarchyTypes) {
    <p>
        @Html.ActionLink((string)type.DisplayName,
          "CreateHierarchy",
          new {
              Area = "Nwazet.Commerce",
              id = (string)type.Name,
              ReturnUrl = Request.QueryString["ReturnUrl"] })
    </p>
}