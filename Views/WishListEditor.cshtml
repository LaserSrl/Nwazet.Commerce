@{
    Script.Require("jQuery");
    Style.Require("Nwazet.WishLists");

    var productId = (int)Model.ProductId;
    var creationShapes = (IEnumerable<dynamic>)Model.CreationShapes;
}

@using (Html.BeginFormAntiForgeryPost(
                Url.Action("Create", "WishLists", new { area = "Nwazet.Commerce" }),
                FormMethod.Post)) {

    <div class="wish-list-editor">
        <div>
            <label for="title">@T("Name of the wish list.")</label>
            <input class="text large" id="title" name="title" type="text" value="">
            @foreach (var cShape in creationShapes) {
                @Display(cShape)
            }
            <input id="button-create-wishlist" type="submit" value="@T("Create WishList")" />

        </div>
    </div>
}
